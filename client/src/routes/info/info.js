import React from 'react';
import { connect } from 'dva';
import './info.css';

const Info = () => {

  return (
    <div>info</div>
  )
}

function mapStateToProps() {
  
}

export default connect()(Info);
